# Project Rules & Conventions (GEMINI.md)

このドキュメントは、本プロジェクト「palbato」の設計思想、アーキテクチャ、およびコーディングルールをまとめたものです。

## 1. アーキテクチャ方針 (Domain-Driven Design)
プロジェクトはDDD（ドメイン駆動設計）の思想に基づき、レイヤーに分離されています。

## 2. コーディングルール
- **ディレクトリ制約**:
    *   **いかなる理由があっても、本プロジェクトのワークスペースより上の階層（親ディレクトリ）にアクセスしたり、操作を行ったりしないでください。**
    *   Git操作、ファイル検索、書き込み等はすべてこのワークスペース内に限定してください。
- **TypeScriptの活用**: 型定義を徹底し、`models.ts` で定義されたインターフェースを使用します。
- **コメントの維持**: 既存の日本語コメントやJSDocは、コード変更時も極力維持してください。
- **マジックナンバーの排除**:
    *   シミュレーションの挙動に影響する数値は `config.ts` に集約し、ハードコードを避けます。
- **非破壊的な変更**:
    *   既存のセーブデータ（SQLite）との互換性を考慮し、スキーマ変更時はデフォルト値の設定などの配慮を行います。

## 3. シミュレーション仕様

## 4. ビルド・デプロイ
- **esbuild**: 高速なビルドのために `esbuild` を使用します。
- **ディレクトリ構造**:
    *   `public/`: 静的ファイル、ビルド済みJS (`assets/main.js`)、index.html。
    *   `src/`: ソースコード。

## 5. 開発の進め方
- 機能追加の際は、まず `implementation_plan.md` で設計方針をユーザーに提示し、承認を得てから実装を開始します。
- **テストの徹底**:
    *   機能追加・改変時には必ずユニットテストを作成してください。
    *   実装完了後は必ずユニットテストを実行し、動作を確認してください。
- **ビルドと確認**:
    *   実装完了後はビルドを確認します。
    *   **TypeScriptファイル (.ts) を更新した際は、例外なく必ず `npm run build` を実行して `main.js` を最新の状態に更新してください。**
- **コミットとプッシュ**:
    *   機能追加・改変の完了後、`git commit & push` を行う際は必ず Yes/No でユーザーに確認をとってください。
    *   **一連の作業（実装・テスト・ビルド）が完了したら、速やかにコミットとプッシュまで到達させてください。**
    *   **一連の変更を1つのコミットに集約**:
        *   関連するソースコードの変更（`src/`）とビルド成果物（`public/assets/main.js`）は、必ず1つのコミットにまとめてください。作業ディレクトリに関わらず、プロジェクト全体の状態を確認して `git add` を行ってください。
    *   **Gitのコミットメッセージは必ず英語で記述してください。**
- **ブラウザテスト**:
    *   明示的な指示がない限り、ブラウザテストは実行しないでください。

## 6. 開発の完結（Strict Cycle）
作業を中断・終了する際は、以下のサイクルが完了していることを自己点検してください。
1.  **Test**: `npm test` がすべてパスしているか？
2.  **Build**: `npm run build` を実行し、`main.js` が更新されているか？
3.  **Commit & Push**: 変更がリモートリポジトリに反映されているか？
これらが欠けている状態での作業終了は、本プロジェクトでは「未完了」とみなされます。
